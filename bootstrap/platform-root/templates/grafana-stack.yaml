{{- /* observability: downstream can disable via components.grafana: false */ -}}
{{- if ne (index .Values.components "grafana") false }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: grafana-db
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "8"
spec:
  project: platform
  source:
    repoURL: {{ .Values.platformRepoUrl }}
    targetRevision: {{ .Values.platformVersion }}
    path: core/components/grafana-db
    {{- if .Values.global.provenance.gitRevision }}
    helm:
      parameters:
        {{- include "platform-root.provenanceParameters" . | nindent 8 }}
    {{- end }}
  destination:
    server: https://kubernetes.default.svc
    namespace: cnpg-system
  syncPolicy:
    retry:
      limit: 10
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 3m
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: grafana
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "8"
spec:
  project: platform
  sources:
    - repoURL: https://grafana.github.io/helm-charts
      chart: grafana
      targetRevision: "10.5.15"
      helm:
        valueFiles:
          - $values/core/components/grafana-stack/grafana-values.yaml
          - $values/core/components/grafana-stack/grafana-values-{{ .Values.global.provider }}.yaml
          {{- include "platform-root.overrideValueFile" (dict "component" "grafana" "root" $) | nindent 10 }}
        {{- include "platform-root.ignoreMissingValueFiles" $ | nindent 8 }}
        valuesObject:
          {{- /* Scheduling (ADR 0012 — issue #97) */}}
          {{- include "platform-root.schedulingValuesTopLevel" (dict
                "mode" (default "auto" (index .Values.scheduling "grafana"))
             ) | nindent 10 }}
    - repoURL: {{ .Values.platformRepoUrl }}
      targetRevision: {{ .Values.platformVersion }}
      ref: values
    {{- include "platform-root.overrideSource" . | nindent 4 }}
  destination:
    server: https://kubernetes.default.svc
    namespace: grafana
  syncPolicy:
    {{- include "platform-root.managedNamespaceMetadataExcludedPrivileged" . | nindent 4 }}
    retry:
      limit: 10
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 3m
    syncOptions:
      - CreateNamespace=true
{{- end }}
---
{{- /* observability: downstream can disable via components.loki: false */ -}}
{{- if ne (index .Values.components "loki") false }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: grafana-loki
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "8"
spec:
  project: platform
  sources:
    - repoURL: https://grafana.github.io/helm-charts
      chart: loki
      targetRevision: "6.54.0"
      helm:
        valueFiles:
          - $values/core/components/grafana-stack/loki-values.yaml
          - $values/core/components/grafana-stack/loki-values-{{ .Values.global.provider }}.yaml
          {{- include "platform-root.overrideValueFile" (dict "component" "loki" "root" $) | nindent 10 }}
        {{- include "platform-root.ignoreMissingValueFiles" $ | nindent 8 }}
        valuesObject:
          {{- /* Scheduling (ADR 0012 — issue #97) — loki-distributed per-component */}}
          {{- $mode := default "auto" (index .Values.scheduling "loki") }}
          {{- $modeIngester := default "auto" (index .Values.scheduling "loki-ingester") }}
          {{- include "platform-root.schedulingValuesFor" (dict
                "mode" $mode
                "paths" (list "gateway" "distributor" "querier" "queryFrontend" "queryScheduler" "ruler" "compactor" "indexGateway")
             ) | nindent 10 }}
          {{- include "platform-root.schedulingValuesFor" (dict
                "mode" $modeIngester
                "paths" (list "ingester")
             ) | nindent 10 }}
        {{- if eq .Values.global.provider "azure" }}
        parameters:
          - name: loki.storage.azure.accountName
            value: "{{ .Values.global.storageAccountName }}"
          - name: serviceAccount.annotations.azure\.workload\.identity/client-id
            value: "{{ .Values.identity.loki.clientId }}"
        {{- end }}
        {{- /* AWS: S3 bucket + IRSA wiring read from loki-values-aws.yaml — no helm parameters needed here. */}}
    - repoURL: {{ .Values.platformRepoUrl }}
      targetRevision: {{ .Values.platformVersion }}
      ref: values
    {{- include "platform-root.overrideSource" . | nindent 4 }}
  destination:
    server: https://kubernetes.default.svc
    namespace: grafana
  syncPolicy:
    {{- include "platform-root.managedNamespaceMetadataExcludedPrivileged" . | nindent 4 }}
    retry:
      limit: 10
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 3m
    syncOptions:
      - CreateNamespace=true
{{- end }}
---
{{- /* observability: downstream can disable via components.mimir: false */ -}}
{{- if ne (index .Values.components "mimir") false }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: grafana-mimir
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "8"
spec:
  project: platform
  sources:
    - repoURL: https://grafana.github.io/helm-charts
      chart: mimir-distributed
      targetRevision: "6.0.5"
      helm:
        valueFiles:
          - $values/core/components/grafana-stack/mimir-values.yaml
          - $values/core/components/grafana-stack/mimir-values-{{ .Values.global.provider }}.yaml
          {{- include "platform-root.overrideValueFile" (dict "component" "mimir" "root" $) | nindent 10 }}
        {{- include "platform-root.ignoreMissingValueFiles" $ | nindent 8 }}
        valuesObject:
          {{- /* Scheduling (ADR 0012 — issue #97) — mimir-distributed per-component */}}
          {{- $mode := default "auto" (index .Values.scheduling "mimir") }}
          {{- $modeIngester := default "auto" (index .Values.scheduling "mimir-ingester") }}
          {{- include "platform-root.schedulingValuesFor" (dict
                "mode" $mode
                "paths" (list "gateway" "distributor" "querier" "query_frontend" "query_scheduler" "ruler" "compactor" "store_gateway" "alertmanager" "overrides_exporter")
             ) | nindent 10 }}
          {{- include "platform-root.schedulingValuesFor" (dict
                "mode" $modeIngester
                "paths" (list "ingester")
             ) | nindent 10 }}
        {{- if eq .Values.global.provider "azure" }}
        parameters:
          - name: mimir.structuredConfig.common.storage.azure.account_name
            value: "{{ .Values.global.storageAccountName }}"
          - name: serviceAccount.annotations.azure\.workload\.identity/client-id
            value: "{{ .Values.identity.mimir.clientId }}"
        {{- end }}
        {{- /* AWS: S3 bucket + IRSA wiring read from mimir-values-aws.yaml — no helm parameters needed here. */}}
    - repoURL: {{ .Values.platformRepoUrl }}
      targetRevision: {{ .Values.platformVersion }}
      ref: values
    {{- include "platform-root.overrideSource" . | nindent 4 }}
  destination:
    server: https://kubernetes.default.svc
    namespace: grafana
  syncPolicy:
    {{- include "platform-root.managedNamespaceMetadataExcludedPrivileged" . | nindent 4 }}
    retry:
      limit: 10
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 3m
    syncOptions:
      - CreateNamespace=true
{{- end }}
---
{{- /* observability: downstream can disable via components.alloy: false */ -}}
{{- if ne (index .Values.components "alloy") false }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: alloy-rbac
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "9"
spec:
  project: platform
  source:
    repoURL: {{ .Values.platformRepoUrl }}
    targetRevision: {{ .Values.platformVersion }}
    path: core/components/alloy-rbac
  destination:
    server: https://kubernetes.default.svc
    namespace: grafana
  syncPolicy:
    retry:
      limit: 10
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 3m
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: grafana-alloy
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "8"
spec:
  project: platform
  sources:
    - repoURL: https://grafana.github.io/helm-charts
      chart: alloy
      targetRevision: "1.6.2"
      helm:
        valueFiles:
          - $values/core/components/grafana-stack/alloy-values.yaml
          - $values/core/components/grafana-stack/alloy-values-{{ .Values.global.provider }}.yaml
          {{- include "platform-root.overrideValueFile" (dict "component" "alloy" "root" $) | nindent 10 }}
        {{- include "platform-root.ignoreMissingValueFiles" $ | nindent 8 }}
        valuesObject:
          {{- /* Scheduling (ADR 0012 — issue #97)
                alloy chart 1.6.2: tolerations/affinity live under `controller.*`
                (the DaemonSet wrapper), not `alloy.*`. */}}
          {{- include "platform-root.schedulingValuesFor" (dict
                "mode" (default "auto" (index .Values.scheduling "alloy"))
                "paths" (list "controller")
             ) | nindent 10 }}
    - repoURL: {{ .Values.platformRepoUrl }}
      targetRevision: {{ .Values.platformVersion }}
      ref: values
    {{- include "platform-root.overrideSource" . | nindent 4 }}
  destination:
    server: https://kubernetes.default.svc
    namespace: grafana
  syncPolicy:
    {{- include "platform-root.managedNamespaceMetadataExcludedPrivileged" . | nindent 4 }}
    retry:
      limit: 10
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 3m
    syncOptions:
      - CreateNamespace=true
{{- end }}
---
{{- /* observability: downstream can disable via components.tempo: false */ -}}
{{- if ne (index .Values.components "tempo") false }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: grafana-tempo
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "8"
spec:
  project: platform
  sources:
    - repoURL: https://grafana.github.io/helm-charts
      chart: tempo
      targetRevision: "1.10.3"
      helm:
        valueFiles:
          - $values/core/components/grafana-stack/tempo-values.yaml
          - $values/core/components/grafana-stack/tempo-values-{{ .Values.global.provider }}.yaml
          {{- include "platform-root.overrideValueFile" (dict "component" "tempo" "root" $) | nindent 10 }}
        {{- include "platform-root.ignoreMissingValueFiles" $ | nindent 8 }}
        valuesObject:
          {{- /* Scheduling (ADR 0012 — issue #97) */}}
          {{- include "platform-root.schedulingValuesTopLevel" (dict
                "mode" (default "auto" (index .Values.scheduling "tempo"))
             ) | nindent 10 }}
    - repoURL: {{ .Values.platformRepoUrl }}
      targetRevision: {{ .Values.platformVersion }}
      ref: values
    {{- include "platform-root.overrideSource" . | nindent 4 }}
  destination:
    server: https://kubernetes.default.svc
    namespace: grafana
  syncPolicy:
    {{- include "platform-root.managedNamespaceMetadataExcludedPrivileged" . | nindent 4 }}
    retry:
      limit: 10
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 3m
    syncOptions:
      - CreateNamespace=true
{{- end }}
---
{{- /* observability: downstream can disable via components.pyroscope: false */ -}}
{{- if ne (index .Values.components "pyroscope") false }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: grafana-pyroscope
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "8"
spec:
  project: platform
  sources:
    - repoURL: https://grafana.github.io/helm-charts
      chart: pyroscope
      targetRevision: "1.19.0"
      helm:
        valueFiles:
          - $values/core/components/grafana-stack/pyroscope-values.yaml
          - $values/core/components/grafana-stack/pyroscope-values-{{ .Values.global.provider }}.yaml
          {{- include "platform-root.overrideValueFile" (dict "component" "pyroscope" "root" $) | nindent 10 }}
        {{- include "platform-root.ignoreMissingValueFiles" $ | nindent 8 }}
        valuesObject:
          {{- /* Scheduling (ADR 0012 — issue #97)
                pyroscope chart (1.19.0) nests tolerations/affinity under
                pyroscope.* key, not top-level. */}}
          {{- include "platform-root.schedulingValuesFor" (dict
                "mode" (default "auto" (index .Values.scheduling "pyroscope"))
                "paths" (list "pyroscope")
             ) | nindent 10 }}
    - repoURL: {{ .Values.platformRepoUrl }}
      targetRevision: {{ .Values.platformVersion }}
      ref: values
    {{- include "platform-root.overrideSource" . | nindent 4 }}
  destination:
    server: https://kubernetes.default.svc
    namespace: grafana
  syncPolicy:
    {{- include "platform-root.managedNamespaceMetadataExcludedPrivileged" . | nindent 4 }}
    retry:
      limit: 10
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 3m
    syncOptions:
      - CreateNamespace=true
{{- end }}
---
{{- /* observability: downstream can disable via components.mimir-rules: false */ -}}
{{- if ne (index .Values.components "mimir-rules") false }}
{{- if ne (index .Values.components "mimir") false }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: mimir-rules
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "8"
spec:
  project: platform
  sources:
    - repoURL: {{ .Values.platformRepoUrl }}
      targetRevision: {{ .Values.platformVersion }}
      path: core/components/mimir-rules
      helm:
        valueFiles:
          - $values/core/components/mimir-rules/values.yaml
          {{- include "platform-root.overrideValueFile" (dict "component" "mimir-rules" "root" $) | nindent 10 }}
        {{- include "platform-root.ignoreMissingValueFiles" $ | nindent 8 }}
        {{- include "platform-root.provenanceParametersBlock" $ | nindent 8 }}
    - repoURL: {{ .Values.platformRepoUrl }}
      targetRevision: {{ .Values.platformVersion }}
      ref: values
    {{- include "platform-root.overrideSource" . | nindent 4 }}
  destination:
    server: https://kubernetes.default.svc
    namespace: grafana
  syncPolicy:
    retry:
      limit: 10
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 3m
{{- end }}
{{- end }}
