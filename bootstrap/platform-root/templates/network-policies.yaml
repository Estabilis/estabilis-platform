{{- /* optional: downstream can disable via components.network-policies: false */ -}}
{{- if ne (index .Values.components "network-policies") false }}
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: network-policies
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "10"
spec:
  project: platform
  sources:
    - repoURL: {{ .Values.platformGitopsRepoUrl }}
      targetRevision: {{ .Values.platformGitopsVersion }}
      path: components/network-policies
      helm:
        valueFiles:
          - $values/components/network-policies/values.yaml
          - $values/values/platform/network-policies.yaml
          {{- include "platform-root.overrideValueFile" (dict "component" "network-policies" "root" $) | nindent 10 }}
          {{- include "platform-root.gitopsValueFile" (dict "component" "network-policies" "root" $) | nindent 10 }}
        ignoreMissingValueFiles: true
        {{- include "platform-root.provenanceParametersBlock" $ | nindent 8 }}
    - repoURL: {{ .Values.platformGitopsRepoUrl }}
      targetRevision: {{ .Values.platformGitopsVersion }}
      ref: values
    {{- include "platform-root.overrideSource" . | nindent 4 }}
    {{- include "platform-root.gitopsSource" . | nindent 4 }}
  destination:
    server: https://kubernetes.default.svc
    namespace: default
  ignoreDifferences:
    - jqPathExpressions:
        - .metadata.annotations["estabilis.io/built-at"]
        - .metadata.annotations["estabilis.io/build-id"]
        - .metadata.annotations["estabilis.io/git-revision"]
        - .metadata.annotations["estabilis.io/git-source"]
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      limit: 10
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 3m
    syncOptions:
      - RespectIgnoreDifferences=true
{{- end }}
