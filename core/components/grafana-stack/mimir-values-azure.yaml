# Azure-specific Mimir values
# Loaded in addition to mimir-values.yaml when provider=azure

mimir:
  structuredConfig:
    common:
      storage:
        backend: azure
        azure:
          account_name: ""           # injected via ArgoCD helm.parameters: global.storageAccountName
          endpoint_suffix: blob.core.windows.net
    blocks_storage:
      backend: azure
      azure:
        container_name: mimir-blocks

global:
  podLabels:
    azure.workload.identity/use: "true"

serviceAccount:
  create: true
  annotations:
    azure.workload.identity/use: "true"
    azure.workload.identity/client-id: ""  # injected via ArgoCD helm.parameters: identity.mimir.clientId
