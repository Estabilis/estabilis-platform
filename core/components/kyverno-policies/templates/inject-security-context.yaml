{{- if .Values.policies.inject_security_context.enabled }}
apiVersion: kyverno.io/v1
kind: ClusterPolicy
metadata:
  name: inject-security-context
  annotations:
    policies.kyverno.io/title: Inject Security Context Defaults
    policies.kyverno.io/description: >-
      Automatically injects safe security context defaults on containers that
      do not already define them. Sets runAsNonRoot, readOnlyRootFilesystem,
      allowPrivilegeEscalation, capabilities drop ALL, and seccomp profile.
spec:
  background: true
  rules:
    - name: inject-security-context-pods
      match:
        any:
          - resources:
              kinds:
                - Pod
      exclude:
        any:
          - resources:
              namespaces:
                - kube-system
                - cert-manager
                - cnpg-system
                - external-dns
                - external-secrets
                - grafana
                - kube-state-metrics
                - kyverno
                - node-exporter
                - opencost
                - traefik
                - trivy-system
                - velero
      mutate:
        foreach:
          - list: "request.object.spec.containers[]"
            patchStrategicMerge:
              spec:
                containers:
                  - (name): "{{`{{ element.name }}`}}"
                    securityContext:
                      +(runAsNonRoot): true
                      +(readOnlyRootFilesystem): true
                      +(allowPrivilegeEscalation): false
                      +(capabilities):
                        drop:
                          - ALL
                      +(seccompProfile):
                        type: RuntimeDefault
    - name: inject-security-context-controllers
      match:
        any:
          - resources:
              kinds:
                - Deployment
                - StatefulSet
                - DaemonSet
      exclude:
        any:
          - resources:
              namespaces:
                - kube-system
                - cert-manager
                - cnpg-system
                - external-dns
                - external-secrets
                - grafana
                - kube-state-metrics
                - kyverno
                - node-exporter
                - opencost
                - traefik
                - trivy-system
                - velero
      mutate:
        foreach:
          - list: "request.object.spec.template.spec.containers[]"
            patchStrategicMerge:
              spec:
                template:
                  spec:
                    containers:
                      - (name): "{{`{{ element.name }}`}}"
                        securityContext:
                          +(runAsNonRoot): true
                          +(readOnlyRootFilesystem): true
                          +(allowPrivilegeEscalation): false
                          +(capabilities):
                            drop:
                              - ALL
                          +(seccompProfile):
                            type: RuntimeDefault
    - name: inject-security-context-cronjob
      match:
        any:
          - resources:
              kinds:
                - CronJob
      exclude:
        any:
          - resources:
              namespaces:
                - kube-system
                - cert-manager
                - cnpg-system
                - external-dns
                - external-secrets
                - grafana
                - kube-state-metrics
                - kyverno
                - node-exporter
                - opencost
                - traefik
                - trivy-system
                - velero
      mutate:
        foreach:
          - list: "request.object.spec.jobTemplate.spec.template.spec.containers[]"
            patchStrategicMerge:
              spec:
                jobTemplate:
                  spec:
                    template:
                      spec:
                        containers:
                          - (name): "{{`{{ element.name }}`}}"
                            securityContext:
                              +(runAsNonRoot): true
                              +(readOnlyRootFilesystem): true
                              +(allowPrivilegeEscalation): false
                              +(capabilities):
                                drop:
                                  - ALL
                              +(seccompProfile):
                                type: RuntimeDefault
{{- end }}
