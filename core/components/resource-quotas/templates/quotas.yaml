{{- range $ns, $quota := .Values.namespaces }}
{{- if $quota.enabled }}
---
apiVersion: v1
kind: ResourceQuota
metadata:
  name: platform-quota-{{ $ns }}
  namespace: {{ $ns }}
  labels:
    {{- include "resource-quotas.labels" $ | nindent 4 }}
spec:
  hard:
    {{- if $quota.hard }}
    {{- toYaml $quota.hard | nindent 4 }}
    {{- else }}
    {{- toYaml $.Values.defaults.hard | nindent 4 }}
    {{- end }}
{{- end }}
{{- end }}
