admissionController:
  replicas: 1

backgroundController:
  enabled: true

cleanupController:
  enabled: true

reportsController:
  enabled: true

grafana:
  enabled: true
  namespace: grafana
  labels:
    grafana_dashboard: "1"
  annotations:
    grafana_folder: Kyverno
