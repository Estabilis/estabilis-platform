# grafana-db-credentials in cnpg-system — CNPG uses key "password" to create the role
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: grafana-db-credentials
  namespace: cnpg-system
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: azure-keyvault
    kind: ClusterSecretStore
  target:
    name: grafana-db-credentials
    creationPolicy: Owner
  data:
    - secretKey: password
      remoteRef:
        key: {{ .Values.kvSecrets.grafanaDbPassword | quote }}
---
# argocd-db-credentials in cnpg-system — CNPG uses key "password" to create the role
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: argocd-db-credentials
  namespace: cnpg-system
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: azure-keyvault
    kind: ClusterSecretStore
  target:
    name: argocd-db-credentials
    creationPolicy: Owner
  data:
    - secretKey: password
      remoteRef:
        key: {{ .Values.kvSecrets.argocdDbPassword | quote }}
