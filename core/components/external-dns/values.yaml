commonLabels:
  estabilis.io/managed-by: platform
  estabilis.io/component: external-dns

txtOwnerId: ""  # injected via ArgoCD helm.parameters (global.clusterName)

policy: upsert-only

sources:
  - service
  - ingress

domainFilters: []  # injected via ArgoCD helm.parameters
